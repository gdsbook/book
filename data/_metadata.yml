# The data-cleaning notebooks are reference appendices documenting how each
# dataset was produced. They depend on external downloads/services and are not
# re-executed at build time; they render as static code listings.
execute:
  eval: false
